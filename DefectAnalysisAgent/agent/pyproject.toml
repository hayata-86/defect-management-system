[project]
name = "defect-analysis-agent"
version = "0.1.0"
description = "AI agent for analyzing defect management data"
requires-python = ">=3.12"
dependencies = [
    "bedrock-agentcore==1.18.1",
    "strands-agents==1.50.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"